<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ============ Dark theme (NIGHT mode) ============ -->
    <!-- This is the primary theme matching the original Protect Yourself dark blue look -->
    <style name="Theme.ProtectYourself" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:colorBackground">@color/bg_blue_dark</item>
        <item name="android:windowBackground">@color/bg_blue_dark</item>
        <item name="android:statusBarColor">@color/bg_blue_dark</item>
        <item name="android:navigationBarColor">@color/bg_blue_dark</item>
        <item name="colorAccent">@color/accent_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Activity</item>
    </style>

    <style name="Theme.ProtectYourself.NoActionBar" parent="Theme.ProtectYourself">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.ProtectYourself.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Theme.ProtectYourself.Splash" parent="Theme.ProtectYourself.NoActionBar">
        <item name="android:windowBackground">@color/bg_splash</item>
        <item name="android:statusBarColor">@color/bg_splash</item>
    </style>
</resources>
