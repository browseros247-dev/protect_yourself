<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ============ Brand colors ============ -->
    <color name="accent_color">#FF3ACFFE</color>

    <!-- ============ Dark theme colors (matches original) ============ -->
    <color name="colorPrimary">#FF1F323F</color>

    <color name="bg_blue">#FF151F26</color>
    <color name="bg_blue_dark">#FF061620</color>
    <color name="bg_splash">#FF152B35</color>

    <color name="sub_title_text_color">#FFB7BABD</color>

    <color name="white">#FFFFFFFF</color>
    <color name="black">#FF000000</color>

    <!-- ============ Brand orange gradient ============ -->
    <color name="button_large_gradient_top_default">#FFFF9900</color>
    <color name="button_large_gradient_bottom_default">#FFFF7100</color>

    <!-- ============ Light theme colors (new — DayNight) ============ -->
    <color name="light_bg">#FFF5F7FA</color>

    <!--
        Removed (unused) colors — cleaned up during dead-code analysis:
          - brand_orange, brand_orange_light (Compose theme uses theme/Color.kt BrandOrange instead)
          - colorAccent (Compose theme uses MaterialTheme.colorScheme.primary)
          - bg_blue_dark_overlay (no references)
          - bottom_nav_active, bottom_nav_divider, bottom_nav_inactive, bottom_sheet_divider
            (Compose bottom-nav uses theme/Color.kt)
          - about_message_color, biometric_error_color (no references)
          - light_surface, light_on_bg, light_subtitle, light_bottom_nav_active,
            light_bottom_nav_inactive, light_bottom_nav_divider (Compose theme handles light mode)
          - transparent (no references — Compose uses Color.Transparent)
          - card_background_dark, card_background_light, divider_color_dark, divider_color_light
            (bg_card.xml drawable that used these was also removed)
          - block_screen_background, block_screen_message_text, block_screen_close_button_text,
            block_screen_why_text (page_porn_block.xml uses bg_blue_dark + white directly)
          - amp_blue, amp_dark_blue, amp_gray, amp_light_gray, amp_light_gray_2, amp_transparent
            (AMP SDK was removed; these were "kept for compatibility" but never read)
    -->
</resources>
